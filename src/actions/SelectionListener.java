package actions;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import mode.Mode;
import mode.SelectionMode;

public class SelectionListener extends CustomActionListener{
	public SelectionListener(Mode mode){
		super(mode);
	}
}
